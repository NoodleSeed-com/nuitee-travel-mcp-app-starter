import { starterConfig } from '../../../../starter.config';

export const DARK_THEME_TOKENS = {
  canvas: starterConfig.brand.surfaceDark,
  surface: '#3A4349',
  boundary: '#8798A0',
  focus: starterConfig.brand.signal,
} as const;
