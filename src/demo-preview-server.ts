import { createTravelServer } from './travel-server.js';

/** Credential-free local preview for every Wayfare MCP App widget. */
export default createTravelServer('credential-free', 'expanded-travel');
